name: example
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
    sdk: ^3.8.1

dependencies:
    flutter:
        sdk: flutter
    infinite_lazy_grid:
        path: ..

dev_dependencies:
    flutter_lints: ^6.0.0

flutter:
    uses-material-design: true
